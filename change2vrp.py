from test import nodeneedarray


def change2vrp(Route,nodeneedarray,cap):
    fRoute = []
    sum = 0
    for i in range(Route):
        sum += nodeneedarray

    return fRoute

class nodeneed:
    def __init__(self):
        node = 0
        need = 0